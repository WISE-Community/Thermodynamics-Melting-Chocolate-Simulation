import { HeatingCoolingBarSimulation } from './heatingCoolingBarSimulation';
import * as $ from 'jquery';

$(document).ready(function() {
  let heatingCoolingBarSimulation = new HeatingCoolingBarSimulation();
});
